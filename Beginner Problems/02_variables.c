#include<stdio.h>
int main(){

    //Rules for naming a variable 

    //int 87a;  -> The variable must not start with a number, it can either have an alphabet or an underscore 
    int aL; //  -> VARIABLES ARE CASE SENSITIVE 
    int raj;
    int Raj;
    //int ad s; -> The variable should not contain spaces between them 
    int _hj_as;
    int sd,ds;  //-> When commas are used, the compiler considers them as seperate variables
    //int .dfg; -> We cannot use special characters (except underscore) in a variable
    //int hg@gh;


}